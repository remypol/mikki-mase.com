---
/**
 * PasswordGate — DISABLED (masterclass is now public).
 *
 * Previously provided client-side password protection for partner review.
 * Now acts as a pass-through wrapper that renders content directly.
 *
 * To re-enable: restore from git history.
 */
interface Props {
  password?: string;
}
---

<!-- PasswordGate disabled — content rendered directly -->
<slot />
